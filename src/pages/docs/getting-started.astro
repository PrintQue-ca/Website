---
import Layout from '../../layouts/Layout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const repo = 'https://github.com/PrintQue-ca/PrintQue';
---

<Layout title="Getting started – PrintQue" description="Quick start: clone PrintQue, set up the backend and frontend, and run the application.">
  <div class="mx-auto max-w-4xl px-4 py-12">
    <h1 class="text-3xl font-bold text-neutral-900 dark:text-white mb-2">Getting started</h1>
    <p class="text-neutral-600 dark:text-neutral-400 mb-10">
      Run PrintQue locally with the API server and React frontend.
    </p>

    <section class="mb-10">
      <h2 class="text-xl font-semibold text-neutral-900 dark:text-white mb-4">Prerequisites</h2>
      <ul class="list-disc list-inside text-neutral-600 dark:text-neutral-400 space-y-1 text-sm">
        <li>Node.js 18+ (frontend)</li>
        <li>Python 3.10+ (backend)</li>
        <li>pip</li>
        <li>Network access to your 3D printers</li>
      </ul>
    </section>

    <section class="mb-10">
      <h2 class="text-xl font-semibold text-neutral-900 dark:text-white mb-4">1. Clone the repository</h2>
      <pre class="rounded-lg bg-neutral-100 dark:bg-neutral-800 p-4 text-sm overflow-x-auto"><code>git clone {repo}
cd PrintQue</code></pre>
    </section>

    <section class="mb-10">
      <h2 class="text-xl font-semibold text-neutral-900 dark:text-white mb-4">2. Backend (API server)</h2>
      <pre class="rounded-lg bg-neutral-100 dark:bg-neutral-800 p-4 text-sm overflow-x-auto"><code>cd api
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
python app.py</code></pre>
      <p class="mt-2 text-sm text-neutral-600 dark:text-neutral-400">API runs at <strong>http://localhost:5000</strong></p>
    </section>

    <section class="mb-10">
      <h2 class="text-xl font-semibold text-neutral-900 dark:text-white mb-4">3. Frontend (React app)</h2>
      <p class="text-sm text-neutral-600 dark:text-neutral-400 mb-2">In a separate terminal:</p>
      <pre class="rounded-lg bg-neutral-100 dark:bg-neutral-800 p-4 text-sm overflow-x-auto"><code>cd app
npm install
npm run dev</code></pre>
      <p class="mt-2 text-sm text-neutral-600 dark:text-neutral-400">Frontend runs at <strong>http://localhost:3000</strong></p>
    </section>

    <p class="text-neutral-500 dark:text-neutral-400 text-sm">
      <a href={repo} target="_blank" rel="noopener noreferrer" class="hover:underline">Full README on GitHub</a>
      {' · '}
      <a href={`${base}/download`} class="hover:underline">Download</a>
      {' · '}
      <a href={`${base}/`} class="hover:underline">Home</a>
    </p>
  </div>
</Layout>
